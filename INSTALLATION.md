# Установка mod-hardcore

## Описание
Модуль добавляет режим ХАРДКОР для персонажей с интерактивным меню через спелл.

## Особенности
- 🔴 **ОДНА ЖИЗНЬ** - при смерти персонаж удаляется
- 🚫 **БЕЗ ТОРГОВЛИ** - запрещен аукцион и почта
- 👤 **СОЛО ИГРА** - нельзя вступать в группы
- ⚔️ **БЕЗ ГИЛЬДИИ** - только одиночное прохождение
- 🏆 **ТАБЛИЦА ЛИДЕРОВ** - топ игроков по уровню
- 📢 **ГЛОБАЛЬНЫЕ АНОНСЫ** - оповещения о достижениях
- 🎯 **ИНТЕРАКТИВНОЕ МЕНЮ** - управление через спелл или команду

## Установка

### 1. Клонирование модуля
```bash
cd /path/to/azerothcore/modules
git clone https://github.com/PlayDargoon/mod-hardcore.git
```

### 2. Компиляция
```bash
cd /path/to/build
cmake ..
make -j$(nproc)
make install
```

### 3. SQL
```bash
mysql -u root -p acore_world < /path/to/mod-hardcore/data/sql/db-world/hardcore_spell.sql
```

### 4. Запуск сервера
Просто запустите worldserver - модуль готов к работе!

## Использование

### Доступ к меню

Каждый игрок автоматически получает спелл 38057 при создании персонажа и при входе в игру.

**Два способа открыть меню:**
1. **Команда:** `.menu`
2. **Спелл:** Найти в книге заклинаний и использовать

### Интерактивное меню

Меню показывает:
- Текущий статус режима хардкор
- Доступные действия в зависимости от статуса
- Информацию о правилах
- Таблицу лидеров

**Для игроков 1 уровня (не-хардкор):**
- ✅ **[НАЧАТЬ ИСПЫТАНИЕ]** - активировать режим (подтверждение)
- ℹ️ **Информация о режиме** - правила и награды
- ❌ **Отказаться от испытания** - больше не будет предложений

**Для хардкор игроков:**
- 📊 **Показать мой статус** - уровень, статус жив/мертв
- 🏆 **Таблица лидеров** - топ 10 онлайн игроков
- ℹ️ **Информация о режиме** - справка

**Для отказавшихся:**
- ℹ️ **Информация о режиме** - только просмотр

### Команды

- `.menu` - Открыть интерактивное меню хардкор
- `.hardcore status` - Показать текущий статус
- `.hardcore info` - Показать информацию о режиме
- `.hardcore top` - Таблица лидеров (топ 10)

### Что происходит при смерти?

1. Персонаж получает дебафф "Призрак хардкора"
2. Появляется глобальное объявление о смерти
3. Персонаж автоматически удаляется через 10 секунд
4. Запись сохраняется в истории (можно добавить базу данных погибших героев)

### Ограничения

Игрок в режиме хардкор **НЕ МОЖЕТ**:
- ❌ Использовать аукцион
- ❌ Вступать в группы или рейды
- ❌ Вступать в гильдию
- ❌ Получать почту
- ❌ Входить в подземелья с группой

### Глобальные оповещения

Весь сервер видит:
- 🎯 Когда игрок активирует хардкор
- ⚡ Каждые 5 уровней (5, 10, 15, 20...)
- 💀 Смерть хардкор-игрока
- 🏆 Достижение 60 уровня

## Настройка

Файл конфигурации: `conf/hardcore.conf`

```ini
[hardcore]
Enabled = 1                    # Включить модуль
MaxDeathLevel = 15             # Безопасный уровень (после этого аура спадает)
DungeonCooldown = 86400        # Кулдаун на подземелья (24 часа)
```

## Разработка

### Структура модуля
```
mod-hardcore/
├── src/
│   ├── Hardcore.cpp              # Основная логика
│   ├── Hardcore.h                # Заголовочный файл
│   ├── HardcoreCommandScript.cpp # Команды
│   └── Hardcore_loader.cpp       # Загрузчик
├── conf/
│   └── hardcore.conf.dist        # Конфигурация
└── CMakeLists.txt
```

### Система хуков

Модуль использует следующие хуки:
- `OnLogin` - Показ приглашения
- `OnPlayerKilled` - Обработка смерти
- `OnLevelChanged` - Анонсы уровней
- `CanJoinLfg` - Блокировка LFG
- `OnAddMember` - Блокировка группы/гильдии
- `OnBeforeAuctionHouseMgrSendAuctionList` - Блокировка аукциона
- `OnBeforeMailDraftSendMailTo` - Блокировка почты

## Поддержка

- GitHub: https://github.com/PlayDargoon/mod-hardcore
- Issues: https://github.com/PlayDargoon/mod-hardcore/issues

## Версия

Текущая версия: **v1.4.1**

### История изменений

- **v1.4.1** - Убрана система предметов, добавлено текстовое приглашение
- **v1.4.0** - Переход на встроенное приглашение при логине
- **v1.3.x** - Попытки реализации через предметы
- **v1.2.x** - Система хуков и команд
- **v1.0.0** - Первый релиз

## Лицензия

GNU AGPL v3
