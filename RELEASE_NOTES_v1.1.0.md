# Сводка изменений mod-hardcore v1.1.0

## 📋 Обзор

Версия 1.1.0 значительно расширяет функциональность модуля, добавляя систему команд, полную реализацию кулдаунов подземелий и улучшенную блокировку PvP-функций.

---

## ✨ Новые функции

### 1. Игровые команды (185 строк кода)

**Файл:** `src/HardcoreCommandScript.cpp`

Добавлены две новые команды для игроков:

#### `.hardcore status`
Показывает детальную информацию о состоянии игрока:
- Статус режима (АКТИВЕН/НЕ АКТИВЕН)
- Уровень персонажа
- Состояние (ЖИВ/ПОГИБ НАВСЕГДА)
- Информация об окончательной смерти
- Уровень отключения режима
- Оставшееся время кулдауна подземелий

#### `.hardcore info`
Показывает общую информацию о режиме:
- Описание режима
- Все модификаторы (урон, опыт)
- Список ограничений
- Правила окончательной смерти

### 2. Система кулдаунов подземелий

**Изменённые файлы:**
- `src/Hardcore.h` - Добавлен `HARDCORE_LAST_DUNGEON_TIME`, метод `canEnterDungeon()`
- `src/Hardcore.cpp` - Реализация кулдаунов

**Функциональность:**
- Отслеживание времени последнего входа в подземелье
- Настраиваемый кулдаун (по умолчанию 24 часа)
- Автоматическая телепортация при попытке обойти кулдаун
- Информативные сообщения с точным временем
- Интеграция с командой `.hardcore status`

**Новый хук:**
```cpp
void OnMapChanged(Player* player) override
```

### 3. Улучшенная блокировка PvP

**Новые хуки:**

#### Блокировка полей боя
```cpp
bool CanJoinInBattlegroundQueue(Player* player, ...) override
```

#### Блокировка арены
```cpp
bool CanJoinInArenaQueue(Player* player, ...) override
```

#### Улучшенная блокировка почты
```cpp
void OnBeforeSendMail(Player* player, ...) override  // Блокировка отправки
void OnMailReceive(Player* player, ...) override     // Блокировка получения
```

---

## 📝 Изменённые файлы

### Исходный код

| Файл | Строк (было) | Строк (стало) | Изменение |
|------|--------------|---------------|-----------|
| `Hardcore.h` | 51 | 67 | +16 |
| `Hardcore.cpp` | 726 | 765 | +39 |
| `HardcoreCommandScript.cpp` | 0 | 185 | +185 (новый) |
| **Итого** | 777 | 1017 | **+240** |

### Документация

| Файл | Описание |
|------|----------|
| `README.md` | Добавлена секция "Игровые команды" |
| `CHANGELOG.md` | Версия 1.1.0 с полным списком изменений |
| `COMMANDS.md` | Новый файл - руководство по командам (180+ строк) |
| `TESTING.md` | Новый файл - руководство по тестированию (350+ строк) |
| `DEVELOPMENT.md` | Новый файл - руководство разработчика (300+ строк) |
| `STRUCTURE.md` | Обновлена структура проекта |
| `TODO.md` | Новый файл - планы развития (200+ строк) |

### Конфигурация

| Файл | Изменение |
|------|-----------|
| `CMakeLists.txt` | Добавлен `HardcoreCommandScript.cpp` в сборку |

---

## 🔧 Технические детали

### Новые enum значения

```cpp
enum HardcoreSettings
{
    SETTING_HARDCORE = 0,
    HARDCORE_DEAD = 1,
    HARDCORE_LAST_DUNGEON_TIME = 2  // НОВОЕ
};
```

### Новые методы класса Hardcore

```cpp
bool canEnterDungeon(Player* player, uint32 mapId);  // Проверка кулдауна
```

### Регистрация команд

```cpp
void AddSC_HardcoreCommandScript();  // Объявление
AddSC_HardcoreCommandScript();       // Вызов в AddSC_mod_hardcore()
```

---

## 🐛 Исправленные проблемы

1. **Поля боя не блокировались** - Добавлен правильный хук `CanJoinInBattlegroundQueue`
2. **Арена не блокировалась** - Добавлен хук `CanJoinInArenaQueue`
3. **Почта блокировалась частично** - Добавлена блокировка отправки через `OnBeforeSendMail`
4. **Кулдаун подземелий не работал** - Полностью реализована система через `OnMapChanged`

---

## 📊 Статистика изменений

### Код
- **+240 строк** нового кода
- **+3** новых файла исходного кода
- **+5** новых хуков
- **+2** новые игровые команды
- **+1** новое enum значение
- **+1** новый метод класса

### Документация
- **+5** новых документов
- **~1200 строк** новой документации
- Полностью задокументированы все функции

---

## 🎯 Совместимость

### Требования
- **AzerothCore:** Последняя версия (с поддержкой новых хуков)
- **CMake:** 3.16+
- **Компилятор:** C++17 или новее
- **MySQL:** 5.7+ или MariaDB 10.2+

### Обратная совместимость
- ✅ Совместимо с версией 1.0.0
- ✅ Конфигурационный файл обратно совместим
- ✅ База данных не требует миграции
- ✅ PlayerSettings автоматически добавляет новые поля

---

## 🚀 Установка обновления

### Для новых установок

См. `INSTALL.md` для полной инструкции.

### Для обновления с версии 1.0.0

1. **Остановите сервер**
   ```bash
   # Остановите worldserver
   ```

2. **Обновите файлы модуля**
   ```bash
   cd /path/to/azerothcore-wotlk/modules/mod-hardcore
   git pull origin main
   ```

3. **Пересоберите сервер**
   ```bash
   cd /path/to/azerothcore-wotlk/build
   cmake ../
   make -j$(nproc)
   make install
   ```

4. **Проверьте конфигурацию**
   ```bash
   # Убедитесь, что hardcore.conf актуален
   # Новые параметры уже присутствуют в версии 1.0.0
   ```

5. **Запустите сервер**
   ```bash
   cd /path/to/server/bin
   ./worldserver
   ```

6. **Проверьте в логах**
   ```
   >> Loading mod-hardcore...
   ```

7. **Протестируйте команды**
   ```
   .hardcore status
   .hardcore info
   ```

---

## 🧪 Тестирование

Полное руководство по тестированию: **`TESTING.md`**

### Быстрый тест

1. Войдите в игру хардкор-персонажем
2. Выполните `.hardcore status`
3. Выполните `.hardcore info`
4. Попробуйте войти в подземелье
5. Проверьте кулдаун через `.hardcore status`

### Ожидаемый результат

```
Все команды должны работать без ошибок
Кулдаун должен отображаться корректно
Блокировки БГ/арены должны работать
Почта должна быть полностью заблокирована
```

---

## 📚 Новая документация

### Руководства для игроков
- **COMMANDS.md** - Подробное описание всех команд с примерами

### Руководства для разработчиков
- **DEVELOPMENT.md** - Как расширять и модифицировать модуль
- **TESTING.md** - 15 тестовых сценариев
- **TODO.md** - Планы развития модуля

---

## 🎓 Примеры использования

### Проверка статуса перед рейдом

```
Лидер рейда: .hardcore status
Проверка кулдауна подземелий: 0 ч. 0 мин. (готово)
```

### Помощь новому игроку

```
Новичок: Что такое хардкор?
Ветеран: .hardcore info
[Выводится полная информация о режиме]
```

### Отладка проблем

```
GM: У вас проблемы с хардкором?
Игрок: Да, не могу войти в подземелье
GM: Используйте .hardcore status
[Показывает, что кулдаун активен]
```

---

## 🏆 Благодарности

Спасибо всем, кто помогал с тестированием и предложениями для версии 1.1.0!

---

## 📞 Поддержка

- **GitHub Issues:** [Создать issue]
- **Discord:** [Ссылка на Discord]
- **Форум AzerothCore:** [Ссылка на топик]

---

**Версия:** 1.1.0  
**Дата релиза:** 3 ноября 2025  
**Автор:** AzerothCore Community
