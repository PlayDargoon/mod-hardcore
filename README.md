# ![logo](https://raw.githubusercontent.com/azerothcore/azerothcore.github.io/master/images/logo-github.png) AzerothCore

## mod-hardcore

Модуль хардкор-режима «Без права на ошибку» для AzerothCore (WotLK 3.3.5a).

[![License](https://img.shields.io/badge/license-AGPL--3.0-blue.svg)](LICENSE)
[![AzerothCore](https://img.shields.io/badge/AzerothCore-3.3.5a-brightgreen.svg)](https://www.azerothcore.org/)
[![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20Linux-lightgrey.svg)]()

---

## 📚 Документация

- **[INSTALL.md](INSTALL.md)** - Быстрая установка
- **[CONFIGURATION_EXAMPLES.md](CONFIGURATION_EXAMPLES.md)** - Примеры конфигураций
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений

---

### Описание

Этот модуль добавляет режим **Хардкор «Без права на ошибку»** с единственной жизнью и множественными ограничениями:

**Базовая механика:**
- У персонажа только одна жизнь до максимального уровня
- При смерти персонаж навсегда остается духом
- Автоматический выпуск духа при смерти
- Воскрешение невозможно
- Красная аура для визуальной идентификации
- Активация только на 1 уровне

**Ограничения и ослабления:**
- Наносимый урон: -10% (настраиваемо)
- Получаемый урон: +50% (настраиваемо)
- Рыцари смерти НЕ МОГУТ участвовать
- Блокировка аукциона, почты, гильдейского банка
- Блокировка полей боя и арены
- Принудительный PvE режим
- Группы только с хардкор-игроками (±5 уровней)
- Обмен/дуэли/баффы только с хардкор-игроками
- Кулдаун подземелий: 24 часа

**Оповещения:**
- Глобальные анонсы смерти (от 20+ уровня)
- Анонс при достижении максимального уровня
- Все ограничения снимаются при завершении испытания

### Особенности

1. **Красная аура**: Хардкор-игроки имеют красную ауру (Spell ID 36573 по умолчанию)
2. **Смерть окончательна**: Нет возможности воскреснуть после смерти
3. **Автоматический выпуск духа**: При смерти дух автоматически покидает тело
4. **Глобальные оповещения**: 
   - Сообщение в чат при активации режима
   - Сообщение в чат + на экран при смерти
5. **Защита от обхода**: Блокировка воскрешения через NPC и заклинания

### Установка

#### Шаг 1: Размещение модуля
```bash
# Скопируйте папку mod-hardcore в директорию modules вашего сервера
cp -r mod-hardcore /путь/к/azerothcore/modules/
```

#### Шаг 2: Компиляция сервера
```bash
cd /путь/к/azerothcore/build
cmake ../ -DCMAKE_INSTALL_PREFIX=/путь/к/серверу
make -j$(nproc)
make install
```

**Windows (Visual Studio):**
```powershell
cd C:\azerothcore-wotlk\build
cmake ../ -DCMAKE_INSTALL_PREFIX="C:\azerothcore-server"
# Откройте AzerothCore.sln и соберите проект (Build Solution)
```

#### Шаг 3: Конфигурация
```bash
# Скопируйте конфигурационный файл
cp /путь/к/azerothcore/modules/mod-hardcore/conf/hardcore.conf.dist \
   /путь/к/серверу/etc/modules/hardcore.conf

# Откройте файл и настройте параметры
nano /путь/к/серверу/etc/modules/hardcore.conf
```

**Windows:**
```powershell
# Скопируйте файл конфигурации
copy C:\azerothcore-wotlk\modules\mod-hardcore\conf\hardcore.conf.dist ^
     C:\azerothcore-server\etc\modules\hardcore.conf

# Откройте в блокноте для редактирования
notepad C:\azerothcore-server\etc\modules\hardcore.conf
```

#### Шаг 4: Импорт SQL
```bash
# Подключитесь к базе данных
mysql -u root -p acore_world

# Импортируйте SQL файл
source /путь/к/azerothcore/modules/mod-hardcore/data/sql/db-world/base/hardcore_gameobject.sql;
exit
```

**Windows:**
```powershell
# Используйте HeidiSQL или выполните через командную строку
mysql -u root -p acore_world < C:\azerothcore-wotlk\modules\mod-hardcore\data\sql\db-world\base\hardcore_gameobject.sql
```

#### Шаг 5: Запуск сервера
```bash
# Запустите worldserver
cd /путь/к/серверу/bin
./worldserver

# Проверьте в логах наличие строки:
# >> Loading mod-hardcore...
```

**Windows:**
```powershell
cd C:\azerothcore-server\bin
.\worldserver.exe
```

#### Шаг 6: Проверка работы
1. Войдите в игру
2. Создайте нового персонажа (1 уровень)
3. Найдите объект **"Активация Хардкор"** на кладбище стартовой зоны
4. Взаимодействуйте с объектом
5. Должна появиться красная аура и сообщение об активации

---

### Расположение объектов активации

Объекты **"Активация Хардкор"** размещены на кладбищах следующих локаций:

**Альянс:**
- Нортшир (Эльвинский лес) - `-8913.23, -137.61, 81.86`
- Кхарнос (Дун Морог) - `-6062.91, 331.59, 395.29`
- Амман Вэйл (Azuremyst Isle) - `-4192.62, -12510.7, 44.52`
- Даларан (нейтральное) - `5693.08, 612.97, 647.66`

**Орда:**
- Долина Испытаний (Дуротар) - `-618.52, -4251.23, 38.69`
- Лагерь Наррач (Мулгор) - `-2340.84, -346.06, -8.96`
- Долина Теней (Тирисфаль) - `1859.45, 1567.72, 94.31`
- Остров Зыбителей Солнца (Призрачные земли) - `10377.3, -6373.05, 35.97`

---

### Обновление модуля

Если вы обновляете существующий модуль:

```bash
# 1. Остановите сервер
# 2. Замените файлы модуля
# 3. Пересоберите сервер (см. Шаг 2)
# 4. Проверьте конфигурацию на наличие новых параметров
# 5. Запустите сервер
```

---

### Удаление модуля

```bash
# 1. Остановите сервер
# 2. Удалите объекты из базы данных
mysql -u root -p acore_world
DELETE FROM gameobject WHERE id = 300001;
DELETE FROM gameobject_template WHERE entry = 300001;
exit

# 3. Удалите папку модуля
rm -rf /путь/к/azerothcore/modules/mod-hardcore/

# 4. Удалите конфигурацию
rm /путь/к/серверу/etc/modules/hardcore.conf

# 5. Пересоберите сервер
```

### Конфигурация

Откройте `hardcore.conf` для настройки:

```ini
# Включить/отключить модуль
Hardcore.Enable = 1

# ID заклинания для красной ауры
# Рекомендуемые значения:
#   36573 - Vision (красно-оранжевое свечение) - по умолчанию
#   57940 - Camo (красноватый эффект)
#   23493 - Emberseer (красная аура)
#   32474 - Красный огненный эффект
Hardcore.AuraSpellId = 36573
```

### Использование

1. Создайте нового персонажа (1 уровень или 55 для Рыцаря Смерти)
2. Найдите объект "Активация Хардкор" на кладбище стартовой зоны
3. Взаимодействуйте с объектом для активации режима Хардкор
4. На персонаже появится красная аура
5. При смерти персонаж не сможет быть воскрешен

### Технические детали

- **Типы скриптов**: WorldScript, PlayerScript, GameObjectScript
- **Хранение данных**: PlayerSettings (mod-hardcore)
- **Механика смерти**: `BuildPlayerRepop()` + `RepopAtGraveyard()`
- **Блокировка воскрешения**: `CanRepopAtGraveyard()` + `OnPlayerResurrect()`

### Требования

- AzerothCore (WotLK 3.3.5a)
- CMake 3.16+
- C++17 или новее
- MySQL 5.7+ или MariaDB 10.2+

---

### Устранение неполадок

#### Проблема: Модуль не загружается

**Проверка:**
```bash
# В логах worldserver должна быть строка:
>> Loading mod-hardcore...
```

**Решение:**
1. Убедитесь, что файл `hardcore.conf` находится в `etc/modules/`
2. Проверьте, что параметр `Hardcore.Enable = 1`
3. Пересоберите сервер после добавления модуля

#### Проблема: Объект "Активация Хардкор" не видим

**Причины:**
- SQL файл не импортирован
- Персонаж выше 1 уровня
- Рыцарь смерти (заблокированы по умолчанию)

**Решение:**
```sql
-- Проверьте наличие объектов в базе
SELECT * FROM gameobject WHERE id = 300001;
SELECT * FROM gameobject_template WHERE entry = 300001;

-- Если пусто - импортируйте SQL снова
```

#### Проблема: Красная аура не отображается

**Решение:**
1. Проверьте настройку `Hardcore.AuraSpellId` в конфиге
2. По умолчанию используется Spell ID `36573`
3. Попробуйте альтернативные ID: `57940`, `23493`, `32474`
4. Убедитесь, что персонаж вышел из игры и зашёл снова

#### Проблема: Ошибки компиляции

**Типичные ошибки:**

1. **Undefined reference to AllCreatureScript/GroupScript/BGScript**
   ```
   Решение: Обновите AzerothCore до последней версии
   ```

2. **OnDamageDealt/OnTakeDamage not found**
   ```
   Решение: Эти хуки могут отсутствовать в старых версиях AC
   Закомментируйте эти методы или обновите ядро
   ```

3. **SpellInfo errors**
   ```
   Решение: Проверьте include директивы в Hardcore.h
   Добавьте: #include "SpellInfo.h"
   ```

#### Проблема: Игроки могут воскрешаться

**Проверка:**
```sql
-- Проверьте настройки персонажа
SELECT * FROM character_settings 
WHERE guid = [GUID_персонажа] 
AND source = 'mod-hardcore';
```

**Решение:**
1. Убедитесь, что `CanRepopAtGraveyard` возвращает `false`
2. Проверьте `OnPlayerResurrect` - должен вызывать `KillPlayer()`
3. Проверьте логи на наличие ошибок

#### Проблема: Группы с обычными игроками не блокируются

**Решение:**
1. Убедитесь, что `Hardcore_GroupScript` зарегистрирован в `AddSC_mod_hardcore()`
2. Проверьте, что хук `OnAddMember` срабатывает
3. Добавьте логирование для отладки

#### Проблема: Модификаторы урона не работают

**Проверка настроек:**
```ini
Hardcore.DamageDealtModifier = 0.9   # -10% урона
Hardcore.DamageTakenModifier = 1.5   # +50% получаемого
```

**Решение:**
1. Хуки `OnDamageDealt` и `OnTakeDamage` могут отсутствовать в старых версиях
2. Обновите AzerothCore или используйте альтернативные хуки
3. Закомментируйте эти методы, если они не поддерживаются

---

### Часто задаваемые вопросы (FAQ)

**Q: Можно ли отключить режим после активации?**  
A: Да, но только до первой смерти. Нужно добавить команду `.hardcore off` (TODO).

**Q: Рыцари смерти могут участвовать?**  
A: По умолчанию нет (`Hardcore.BlockDeathKnight = 1`). Можно изменить в конфиге.

**Q: На каких уровнях работает окончательная смерть?**  
A: Настраивается через `Hardcore.DisableLevel` и `Hardcore.MaxDeathLevel`.

**Q: Как изменить множители урона?**  
A: Отредактируйте `Hardcore.DamageDealtModifier` и `Hardcore.DamageTakenModifier` в конфиге.

**Q: Можно ли использовать аукцион/почту?**  
A: По умолчанию нет. Можно разрешить через параметры `Hardcore.BlockAuction` и `Hardcore.BlockMail`.

**Q: Где взять таблицу лидеров?**  
A: Функционал в разработке (TODO).

**Q: Как работают группы в хардкоре?**  
A: Только с другими хардкор-игроками, разница уровней не более 5 (настраивается).

---

### Поддержка и вклад

**Нашли баг?** Создайте issue на GitHub  
**Хотите улучшить модуль?** Pull requests приветствуются!

**Полезные ссылки:**
- [AzerothCore Wiki](https://www.azerothcore.org/wiki/)
- [ScriptMgr Hooks](https://www.azerothcore.org/pages/doxygen/)
- [Module Development Guide](https://www.azerothcore.org/wiki/Create-a-Module)

---

### Лицензия

GNU AGPL v3

### Авторы

Разработано для AzerothCore  
Модуль «Без права на ошибку» - 2025
